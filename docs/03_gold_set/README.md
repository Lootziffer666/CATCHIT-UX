# Gold Set Guidance

## Canonical split
- **Core Gold**: architecturally central references (`12A_*`).
- **Extended Gold**: supporting but still gold-level references (`12B_*`).
- **Masterlist/bridge**: cross-reference inventories (`12C_*`, visual baseline files).

Read this folder after philosophy/system docs and before editing screen-level specs.
